public class stringMultiply {
    //public String multiply(String num1, String num2) {
    //    return null;
    //}
    ////make sure a != "0" when calling this method
    //private String helper(char b, String a){
    //    //corner case for b = 0 or a = 0
    //    StringBuilder sb = new StringBuilder("0");
    //    if(b!='0'){
    //        int carry = 0, mi = 0;
    //        for(char t : new StringBuilder(a).reverse.toString.toCharArray()){
    //            mi = Character.getNumericValue(t)* Character.getNumericValue(b) + carry;
    //            carry = mi/10;
    //            sb.insert(0,mi%10);
    //        }
    //        if(carry !=0) sb.insert(carry);
    //    }
    //    return sb.toString();
    //}
    ////make sure a or b is not empty string when calling this method
    //private String helpAddString(String a,String b){
    //    if(a == "0") return b;
    //    if(b == "0") return a;
    //    StringBuilder sb = new StringBuilder();
    //    int si = 0, carry= 0;
    //    char[] ar = StringBuilder(a).reverse().toString.toCharArray();
    //    char[] br = StringBuilder(a).reverse().toString.toCharArray();
    //    for(i=0;i < Math.max(a.length,b.length);i++) {
    //        if(i > a.length-1){
    //            if(carry == 0)
    //                sb.insert(0,b.subSting(i));
    //            else
    //                sb.insert(0,helperAdd(b.subStirng,carry));
    //            break;
    //        }
    //        if(i > b.length-1){
    //            sb.insert(0,helperAdd(a,carry));
    //            break;
    //        }
    //        si = Character.getNumericValue(ar[i])* Character.getNumericValue(br[i]) + carry;
    //        carry = si/10;
    //        sb.insert(0,si%10);
    //    }
    //    return sb.toString();
    //}
    ////a is not empty string, cy could be 0
    //private String helperAdd(char[] a, int cy){
    //    //if(cy = 0)
    //    return null;
    //}
}
